@{
    Layout = "~/Views/Shared/_LayoutGlobal.cshtml";
    var currentController = ViewContext.RouteData.Values["controller"] as string ?? "Home";
}
<div id="menucontainer">
    <ul id="menu">
        <li>@Html.ActionLink("+ New Centers", "Index", "Centers", null, new { id = currentController == "Centers" ? "current" : ""})</li>
        <li>@Html.ActionLink("Generate Report", "Index","Report", null, new { id = currentController == "Report" ? "current" : ""})</li>
    </ul>
</div>
<div id="main">
    @RenderBody()
</div>
